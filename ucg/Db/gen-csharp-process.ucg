.template off
// merge common steps script
foreach script
	foreach state
		foreach event
			merge ../../steps[@run='$(@run)']
		endfor
	endfor
endfor

foreach script
	//output "$(@name:p)StateMachine.cs"
	include "cs-passive-sm.ucg"
endfor